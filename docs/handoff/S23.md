# S23 引き継ぎ: ルート README.md の新規作成

ユーザー要望「READMEを作成して(コマンドや環境構築手順が分かるように)」に対応。
コード・契約・仕様は一切変更していない(ドキュメント追加のみ)。

## 完了したこと

- `README.md`(ルート、新規): 初見の人間(ブース運営・後任開発者)向けの日本語 README。
  - 概要(Cubeでハノイ、ルール要点)とシステム構成(機材表+ディレクトリ図)
  - 前提ツール(Python 3.12+/uv、Node.js+npm、任意で firebase-tools)
  - 環境構築(`uv sync` / `npm install` ×2。Makefile に install ターゲットが無いため手動と明記)
  - 実カメラ用の追加準備(`generate_tag_sheet.py` による `output/tag_master.json` 生成、
    `service-account.json` の配置。コミット禁止を明記)
  - 起動(開発機は `HANOI_CV=mock make dev`、素の `make dev` は実CV。カメラ無しでは
    実CV初期化が失敗する旨を注記)、画面URL表、`make mock` のコマンド一覧
  - 検証(`make check` の内訳、`make dev` 稼働中に走らせると uvicorn --reload が再起動して
    idle に戻る注意、E2E `node e2e/full-play.mjs`)
  - 環境変数表(HANOI_* / FIRESTORE_EMULATOR_HOST。既定値はソース照合済み。
    `GOOGLE_APPLICATION_CREDENTIALS` を意図的に見ない旨も記載)
  - `scripts/` 一覧、記録画面のデプロイ手順(cloud/record/README.md の要約)、ドキュメント案内

## 契約からの逸脱・追加判断

- なし(契約・コード無変更)。
- `/admin` 画面は未実装のため README には記載しなかった(App.tsx のコメントどおり)。
- LICENSE ファイルが無く package.json も private のため、ライセンス節は設けなかった。

## レビュー

- local-code-review: README 記載の環境変数・既定値・URL・コマンドを全てソースと照合。指摘ゼロ。
- Codex レビュー: Low 1件(「素の make dev は本番アップロード」が無条件に読める。
  実際は `service-account.json` がある場合のみ有効 — uploader.py:117)→ 修正済み。
  ※初回セッションでは Codex が2回ストールし未実施、翌日ユーザー指示で再実行して完了。
- security-review: ドキュメントのみの変更で検出 0 件。
- `make check` 全パス(server 207 / frontend 122 / cloud 7)。

## 既知の問題

- なし。

## 次セッションへの注意

- README はソースの既定値(ポート、環境変数、URL)を写している。該当コードを変更したら
  README も更新すること(特に `server/app/api/main.py` の既定値と Makefile)。
- `frontend/README.md` は Vite テンプレートのまま(今回スコープ外)。気になるなら削除か差し替え。

## 追記(同セッション続き)

- README に「カメラ設営チェック」(画角 px/mm≈2.56・mat=4/4・追従ギャップの確認手順と
  実サーバーログでの最終確認)と「本番前のデータクリア」(reset_plays.py、operations.md の要約)を追加。
- Makefile に `camera-check` ターゲットを追加(`make camera-check [CAMERA=n]` =
  `cd server && uv run python ../scripts/cv_poc.py --camera n --show`)。
  従来のルート `.venv` 経由(scripts/requirements.txt)は不要になり、server の uv 環境で動くことを確認済み。
