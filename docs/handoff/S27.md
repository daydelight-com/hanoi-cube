# S27 引き継ぎ: Pyxel Cube 版(ブラウザ単体で遊べる派生)の仕様書・設計書・開発計画書

本番(8/21-22)終了後の新規トラック。カメラ・AprilTag 付きの箱・iPad を使わず、
Pyxel の 3D 拡張「Pyxel Cube」(kitao/pyxel の `cube` ブランチ、未リリース)で盤面を描画し、
マウス/タッチのドラッグ&ドロップで遊べる版を GitHub Pages で公開する。
本セッションは**仕様書・設計書・開発計画書の作成まで**。実装は未着手。既存コードは一切変更していない。

## 完了したこと

### Pyxel Cube の調査と実機検証(リポジトリ外)

- cube ブランチ(`f731329`, 2026-08-12)は PyPI 未配布・公式 Web 実行経路(Web Launcher / CDN / app2html)
  のどれも対応していない(すべてリリース版 2.9.x の wheel を読む)。GitHub 上に第三者の利用例もゼロ。
- `pip install git+...@cube` は maturin が `python/pyxel/README.md`(`.gitignore` 対象、Makefile の `cp` 前提)を
  見つけられず失敗する。**ビルドは必ず Pyxel の Makefile 経由**。
- `~/workspace/pyxel-cube` に clone し、`make build-wasm` で
  `pyxel-3.0.0-cp311-abi3-emscripten_5_0_3_wasm32.whl` を生成。動作確認用リポジトリ
  `daydelight-com/pyxel-cube-example` を作り、GitHub Pages で cube のサンプル(c01)が動くことをユーザーが確認済み:
  https://daydelight-com.github.io/pyxel-cube-example/
- 必要なランタイム同梱物は `pyxel.js` / `pyxel.css` / `import_hook.py` / `images/`(7点)/ wheel の5種。
  `images/` または `import_hook.py` が欠けると真っ白のまま止まる(ロゴの `load` 待ち / 404)。

### 仕様書・設計書

- `docs/pyxel_app_specification.md`(新規、約560行)。既存 specification.md の派生として、
  スコープ / ディレクトリ構成 / 画面 / ドラッグ&ドロップ / 判定 / 設計 / 配信 / **セットアップ手順(Mac・Windows 併記)** /
  テスト / 非機能 / 要判断 10 件 / マイルストーン P1〜P4 を定義。
- 設計の骨子: `pyxel_app/` に閉じ、`server/app/core/`(judge / score / precompute)は import のみ。
  ランタイム wheel は `pyxel_app/runtime/` にコミットして固定(cube は API 策定中のため CDN 不使用)。
  アプリ開発者は Rust / Emscripten 不要(wheel は GitHub Release 配布)、フルツールチェーンはランタイム更新担当のみ。
- ユーザー決定事項: 配置先は hanoi-cube 内 `pyxel_app/`、ランキング(Firestore)は初回スコープ外、
  **日本語 UI(JA/EN 切替)を最初から含める**(仕様書 §3.6 新設)、**ネイティブ wheel はビルド済みを GitHub Release で配布**。

### 開発計画書

- `docs/pyxel_app_development_plan.md`(新規)。本体 development_plan.md と同じ様式で **P 系列**(P1〜P6、第2段 P7〜P8)。
  handoff は `docs/handoff/P{n}.md`。P2(Pyxel 非依存層)は P1 と並行可。ランタイム更新は通常セッションと分離(§6)。
- 仕様書 §12 のマイルストーンは計画書への参照に置き換えた(二重管理を避ける)。

### レビュー

- local-code-review: 5 件(fps 既定 30 の見落とし / `.pyxapp` 展開後の `sys.path` 経路未記載 /
  winget 複数指定 / パレット番号の断定 / `gamepad` 属性)→ すべて反映済み。
- security-review: コード変更なし(Markdown のみ)のため指摘 0。
- **Codex レビュー: 未実施**(ユーザーの時間都合。2 回目の /finish もユーザー指示で省略)。
- 2 回目の local-code-review: 仕様書 §12 と計画書の P 番号のずれ 1 件 → 修正済み。
- `make check` 全通過(既存コードの変更なし)。

## 契約からの逸脱・追加判断

- 契約変更なし。仕様書 §0 で「既存コード・契約に触れない」を絶対規則として明記。
- 要判断は仕様書 §11 に 10 件。#2(日本語 UI)・#8(wheel 配布)はユーザー決定済み。残る主要な判断は
  #9 Windows での WASM ビルド(未検証のため Mac/CI で行う)と #3 pydantic のロード時間。
- #7: 判定結果の反映規則(`machine.py::_apply_judgement`)を共有関数に切り出したいが、既存コードを触らない方針のため
  初回は `session.py` に同規則を再実装し、照合テストで守る。後日 core 側で関数化する提案を残す。

## 既知の問題

- 仕様書 §8 の Windows 手順は Pyxel 本家の Makefile と CI 設定から組み立てたもので**実機未確認**。
  最初に通す Windows 開発者のフィードバックで §8.2.6 の表を更新すること。
- cube の `Camera` には画面座標→レイの API が無く、ピッキング(§4.3)は自前実装が必要。
- Pyodide 上で pydantic を読み込む設計(§2.2)のため初回ロードが数秒延びる(要判断 #3)。

## 次セッションへの注意

- 次は計画書 §4 の **P1**(handoff は `docs/handoff/P1.md`): `pyxel_app/` の骨組み、`runtime/` のコミット
  (`~/workspace/pyxel-cube/wasm/` から仕様書 §8.2.5 の手順でコピー、`VERSION.md` 作成)、`make pyxel-site`、
  `.github/workflows/pyxel-pages.yml`、`make check` への組み込み、macOS 用ネイティブ wheel の Release 添付、
  **日本語 BDF フォントの選定・ライセンス確認**。Pages のソースは「GitHub Actions」に切り替える。
- ランタイム更新担当の環境メモ(このマシン): rustup は Homebrew keg-only で `~/.zshrc` に PATH 追記済み、
  toolchain は nightly-2026-07-14、emsdk 5.0.3 は `~/emsdk`。
- `pyxel-cube-example` リポジトリは動作確認用として残す(仕様書 §7.3)。
- 本セッションで `pyxel_examples/`(Pyxel の `copy_examples` 生成物、未追跡)をユーザー指示で削除した。
